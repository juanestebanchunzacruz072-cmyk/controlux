
CREATE DATABASE controlux_db;

-- ============================================================
-- 1. TABLA: roles
-- Define los tipos de usuarios (Administrador y Cliente)
-- ============================================================
CREATE TABLE roles (
    id_rol INT AUTO_INCREMENT PRIMARY KEY,
    nombre_rol VARCHAR(50) NOT NULL UNIQUE
) ENGINE=InnoDB;

-- ============================================================
-- 2. TABLA: usuarios
-- Almacena tanto a Administradores como a Clientes Registrados
-- ============================================================
CREATE TABLE usuarios (
    id_usuario INT AUTO_INCREMENT PRIMARY KEY,
    id_rol INT NOT NULL,
    nombre_completo VARCHAR(100) NOT NULL,
    correo VARCHAR(100) NOT NULL UNIQUE,
    telefono VARCHAR(20) NULL,
    password VARCHAR(255) NOT NULL, -- Encriptada
    fecha_registro TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_usuarios_roles
        FOREIGN KEY (id_rol)
        REFERENCES roles(id_rol)
        ON DELETE CASCADE
        ON UPDATE CASCADE
) ENGINE=InnoDB;

-- ============================================================
-- 3. TABLA: categorias
-- Organiza el catálogo de productos de lujo
-- ============================================================
CREATE TABLE categorias (
    id_categoria INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL,
    estado ENUM('Activo', 'Inactivo') DEFAULT 'Activo'
) ENGINE=InnoDB;

-- ============================================================
-- 4. TABLA: productos
-- Inventario de artículos exclusivos (Relojes, Gorras, etc.)
-- ============================================================
CREATE TABLE productos (
    id_producto INT AUTO_INCREMENT PRIMARY KEY,
    id_categoria INT NOT NULL,
    nombre VARCHAR(150) NOT NULL,
    numero_serie VARCHAR(100) UNIQUE NULL,
    precio DECIMAL(12, 2) NOT NULL,
    stock INT NOT NULL DEFAULT 1,
    url_imagen VARCHAR(255) DEFAULT 'default_producto.jpg',
    estado ENUM('Disponible', 'Agotado', 'Reservado') DEFAULT 'Disponible',
    fecha_creacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_productos_categorias
        FOREIGN KEY (id_categoria)
        REFERENCES categorias(id_categoria)
        ON DELETE CASCADE
        ON UPDATE CASCADE
) ENGINE=InnoDB;

-- ============================================================
-- 5. TABLA: pedidos
-- Registra las compras generadas por los clientes hacia WhatsApp
-- ============================================================
CREATE TABLE pedidos (
    id_pedido INT AUTO_INCREMENT PRIMARY KEY,
    id_usuario INT NOT NULL,
    total DECIMAL(12, 2) NOT NULL,
    estado_pedido ENUM('Pendiente WhatsApp', 'Completado', 'Cancelado') DEFAULT 'Pendiente WhatsApp',
    fecha_pedido TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_pedidos_usuarios
        FOREIGN KEY (id_usuario)
        REFERENCES usuarios(id_usuario)
        ON DELETE CASCADE
        ON UPDATE CASCADE
) ENGINE=InnoDB;

-- ============================================================
-- 6. TABLA: detalle_pedidos
-- Guarda los artículos específicos de cada pedido
-- ============================================================
CREATE TABLE detalle_pedidos (
    id_detalle INT AUTO_INCREMENT PRIMARY KEY,
    id_pedido INT NOT NULL,
    id_producto INT NOT NULL,
    cantidad INT NOT NULL DEFAULT 1,
    precio_unitario DECIMAL(12, 2) NOT NULL,
    subtotal DECIMAL(12, 2) NOT NULL,
    CONSTRAINT fk_detalle_pedidos
        FOREIGN KEY (id_pedido)
        REFERENCES pedidos(id_pedido)
        ON DELETE CASCADE
        ON UPDATE CASCADE,
    CONSTRAINT fk_detalle_productos
        FOREIGN KEY (id_producto)
        REFERENCES productos(id_producto)
        ON DELETE CASCADE
        ON UPDATE CASCADE
) ENGINE=InnoDB;

-- ============================================================
-- 7. INSERTAR DATOS INICIALES DE PRUEBA
-- ============================================================

-- Roles
INSERT INTO roles (nombre_rol) VALUES 
('Administrador'),
('Cliente');

-- Usuarios de prueba (Un Admin y un Cliente)
INSERT INTO usuarios (id_rol, nombre_completo, correo, telefono, password) VALUES
(1, 'Administrador JC URBAN', 'admin@jcurban.com', '3001234567', '$2y$10$e0MYzXyjpJS7Pd0RVvHwHe1z.Yx6c9B4QY3J0iP5O9Z8P7W6e5r4q'),
(2, 'Cliente VIP Prueba', 'cliente@jcurban.com', '3109876543', '$2y$10$e0MYzXyjpJS7Pd0RVvHwHe1z.Yx6c9B4QY3J0iP5O9Z8P7W6e5r4q');

-- Categorías
INSERT INTO categorias (nombre, estado) VALUES
('Relojes', 'Activo'),
('Perfumes', 'Activo'),
('Accesorios de Lujo', 'Activo'),
('Gorras', 'Activo'),
('Joyería', 'Activo');

-- Productos en tendencia
INSERT INTO productos (id_categoria, nombre, numero_serie, precio, stock, url_imagen, estado) VALUES
(1, 'Richard Mille RM011', 'RM011-0089', 4200000.00, 2, 'richard_mille_rm011.jpg', 'Disponible'),
(4, 'Gorra Amiri Black', 'AMR-CAP-001', 280000.00, 5, 'gorra_amiri_black.jpg', 'Disponible'),
(2, 'Sauvage Dior 100ml', 'DIOR-SAV-100', 650000.00, 10, 'sauvage_dior.jpg', 'Disponible'),
(5, 'Cadena Cubana 18k', 'CC18K-0023', 450000.00, 3, 'cadena_cubana_18k.jpg', 'Disponible');